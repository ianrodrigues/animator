# Animator - Coding Exercise


### Instalation

Assuming you have [Composer](https://getcomposer.org/) installed, run:

```bash
composer install
```

### Tests

After installing the dependencies, type this in your terminal to run the tests:

```
./vendor/bin/phpunit
```
